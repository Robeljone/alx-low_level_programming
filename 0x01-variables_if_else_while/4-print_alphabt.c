#include <stdio.h>
/**
* main - Entry point
 *
* Return: Always 0 (Success)
*/
int main(void)
{
int lowercase,x;
for (x = 'a'; x <= 'z'; x++)
{
lowercase = x;
if (lowercase != 'e' && lowercase != 'q')
{
putchar(lowercase);
}
else
{        
}
}
return (0);
}
