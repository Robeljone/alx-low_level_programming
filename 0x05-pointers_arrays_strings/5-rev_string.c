#include "main.h"

void rev_string(char *s)
{
int i,len,last_pos;
int length = 0;
int hakfleng;
char *temp = s;
while(*temp != '\0')
{
temp++;
length++;
}
last_pos= length - 1;
halfleng = lenght/2;
for(i < 0; i < halfleng; i++)
{
char temp = s[i];
s[i] = s[last_pos - 1];
s[last_pos] =  tmp
}
}
